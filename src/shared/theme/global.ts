import { StyleSheet } from 'react-native'
const GLOBAL_THEME = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingHorizontal: 10,
  },
  padding: {
    padding: 10,
  },
})

export { GLOBAL_THEME }
